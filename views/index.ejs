<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LOGIN</title>
    <style>
        body {
            font-family: sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #f4f4f4;
            background-image: url('https://rockwellautomation.scene7.com/is/image/rockwellautomation/5120x2934-backTexture-af2023.5120.jpg');
            background-size: cover;
            background-position: center;
            height: 100vh;
            position: relative;
            overflow: hidden; /* Hide scroll bars initially */
        }
        
        .container {
            position: relative; /* Add this line */
            z-index: 1; /* Add this line */
            width: 80%;
            max-width: 350px;
            background-color: rgba(255, 255, 255, 0.8);
            padding: 20px;
            border-radius: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        .logo {
            width: 150px;
            margin-bottom: 20px;
        }
        h3 {
            margin-bottom: 20px;
            font-family: sans-serif;
        }
        label {
            font-weight: bold;
            color: #555;
            text-align: left;
            display: block;
        }
        input[type="text"],
        input[type="password"],
        button {
            width: calc(100% - 22px);
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-family: sans-serif;
            color: #333;
        }
        button {
            width: 35%;
            background-color: rgba(233, 117, 16, 0.849);
            color: #fff;
            cursor: pointer;
            border-radius: 20px;
            padding: 8px 16px;
            margin-left: auto;
            margin-right: auto;
            display: block;
        }
        button:hover {
            background-color: #9e3d00;
        }
        .loading {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.8); /* Semi-transparent black */
            display: none;
            z-index: 2;
            color: #fff;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            font-size: 20px;
            display: none;
        }
        .loading-text {
            font-family: "Arial", sans-serif;
            font-size: 18px;
            color: #fff;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }
        
        .loading .spinner {
            width: 40px;
            height: 40px;
            border: 4px solid #fff;
            border-top: 4px solid rgba(233, 117, 16, 0.849);
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin-bottom: 20px;
        }
        .loading p {
            margin: 0;
            font-size: 18px;
        }
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
    </style>
</head>
<body>
    <div class="container">
        <img src="https://www.rockwellautomation.com/content/dam/rockwell-automation/sites/images/logos/2019_Logo_rgb_RA_Bug-LeftText_color.svg" alt="Logo" class="logo">
        <h3>Login to the Dashboard</h3>
        <% if (message) { %>
            <p style="color: red;"><%= message %></p>
        <% } %>
        <form action="/login" method="POST" onsubmit="showLoading();logClientSide('Login attempt');">
            <!-- <label for="username">Username:</label> -->
            <input type="text" id="username" name="username" placeholder="username@email.com" required>
            <!-- <label for="password">Password:</label> -->
            <input type="password" id="password" name="password" placeholder="Password" required>
            <button type="submit">Login</button>
        </form>
    </div>
    <div class="loading">
        <div class="spinner"></div>
        <p class="loading-text">Validating...Please Wait</p>
    </div>
    
    <script>
        function showLoading() {
            document.querySelector('.loading').style.display = 'flex';
            document.body.style.overflow = 'hidden';
        }
               // Function to hide the loading screen
        function hideLoading() {
        document.querySelector('.loading').style.display = 'none'; // Hide loading screen
        document.body.style.overflow = ''; // Re-enable scrolling
        }
    </script>
</body>
</html>
